# LM Studio + New Panel Mockup

Static review artifact for issue #2444.

- Open `index.html` or `desktop.png` to inspect the integrated SerenDesktop workspace mockup.
- The sidebar `+ New` dropdown places `LM Studio Agent` in `Coding agents`, after Gemini, with a green `Local` chip.
- The active pane mirrors the real agent chat shell without adding a fake top-tab thread strip: action header, transcript rows, CWD strip, composer toolbar, provider switcher, and open agent model selector.
- Review focus: row label/subtitle, placement, `Local` chip treatment, and whether the LM Studio model-selector state needs different copy or affordance before PR.
